import { MAP_TYPES } from '@deck.gl/carto';

const STARTUPS3_SOURCE_ID = 'startups3Source';

const source = {
  id: STARTUPS3_SOURCE_ID,
  type: MAP_TYPES.TILESET,
  data: `LEEP Community Map`,
};

export default source;
