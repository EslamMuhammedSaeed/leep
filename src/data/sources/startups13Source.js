import { MAP_TYPES } from '@deck.gl/carto';

const STARTUPS13_SOURCE_ID = 'startups13Source';

const source = {
  id: STARTUPS13_SOURCE_ID,
  type: MAP_TYPES.QUERY,
  data: `testing_dataset_for_sdgs_widget_2`,
};

export default source;
